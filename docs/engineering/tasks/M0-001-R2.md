# M0-001 revision 2 — artwork and first-candidate repairs

Scope: complete the first native checkpoint with requested local Workshop logos
and fix confirmed independent QA findings in the initial candidate. The original
ARCH-001 A1–A5 acceptance remains; add ARCH-002's four-logo and parser/concurrency
checks. Load-order automation is not part of this revision.

Bindings:
- ARCH-001 revision 2, SHA256
  `ec9dced52ef371b2915de8a7fab737ee357dedc4baf435784967b6676bbf8619`,
  independent pass AR-001-R2.
- ARCH-002 revision 1, SHA256
  `862707999aa789fe4092fff45cfeb98ecb84fd8993fae6a6e4dadbd6b39acacf`,
  independent pass AR-002-R1, including all implementation obligations.

Owner: Sol Medium, no child agents. One code writer; no other contributor's edits
may be reverted. Existing candidate is parked at `.build/evidence/M0-001-A1/`.
Product source, tests, Xcode project and scripts belong to the implementer; root
owns all Markdown/board/Git. Mutable build outputs are exclusive to implementer
until submission. No app automation, game launch, Steam/game writes, addon copies,
network image fetching, commits, pushes, permission changes or global settings.

Implement a bounded in-memory local PAA/PBO artwork path. Follow ARCH-002 exactly,
including actual cancellation propagation and a global two-reader bound across
generations. Ordinary encoded image caps: 16,384 pixels per dimension and
64 megapixels, in addition to the 8 MiB source cap; thumbnail target 128 pixels.
The 128-entry cache contains decoded thumbnails only. Surface a short truthful
advanced note that mod folders use selection order and dependency checks are not
available; no promise of automatic sorting or compatibility.

Repair original correctness findings within the adopted ARCH-001 contract; no
new architecture is authorized through a repair. Gate must parse each current
task binding, pass/adoption records and exact hashes. Validate maintained local
Markdown links (exclude immutable review snapshots and reusable templates),
physical line limits including Package.swift, strict Swift formatting, Swift
tests with warnings as errors, and Xcode Debug build with warnings as errors.
Negative tests validate the gate itself, including unrelated historical matches.

Tests use synthetic fixtures, no real logos copied into the repo. Extend the
read-only diagnostics to report thumbnail outcomes/dimensions and exercise all
four real installed addons. Run the full gate, retain actual log paths/statuses
and source manifest, then relinquish source/build ownership for independent QA.
Do not mark game loading or native visual acceptance passed from these checks.

Bound: 45 minutes / 140 calls; same M0-001 implementation incident, attempt A2,
first of up to three ordinary repairs. Report remaining blockers rather than
weaken checks. Any substantial architecture question returns to the lead.
