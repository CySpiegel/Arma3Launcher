# M0-001 — First native test application

Status: draft packet; not eligible until the board binds a passing independent
ARCH-001 review and the lead adopts its exact revision. The board is authoritative
for claim, attempts, candidates, findings and acceptance state.

## Objective and ownership

Build the first real native macOS configuration-to-launch checkpoint from
ARCH-001 and the design brief. The user authorized construction and wants to be
notified when a tested app can be opened. Addons stay in Steam's Workshop folder:
read original files in place, never copy, move, stage, edit or create symlinks.

One Sol Medium implementation worker exclusively owns product paths:
`Package.swift`, `Sources/`, `Tests/`, `App/`, `Arma3Launcher.xcodeproj/`,
`scripts/`, `.gitignore`, `.swift-format`, and ignored `.build/` outputs/logs.
Do not edit `AGENTS.md`, README, docs, board, Git metadata, or global settings.
The lead owns coordination/documents; the worker is not alone in the workspace
and must preserve others' edits. No child agents.

Branch: `issue/m0/001-foundation`. Milestone: M0 / `milestone/m0`.
Base: `a3ef844f4c9035e590c6d1f2dddb270ac9a50cf7` (empty initial ancestry).
No remote PR exists; publishing/integration is separate authority. The worker
must not commit, push, create remotes, switch branches, release, or deploy.

## Inputs, dependencies and design

- Bind exact adopted ARCH-001 SHA and review ID in the immutable board revision
  and dispatch packet before Ready/claim. Do not infer approval from this file.
- Read docs/specs/design-brief.md, its main-window-v1.png concept, and
  docs/engineering/evidence/installation-2026-09-19.md.
- Apply CODING-STANDARDS.md and RETRY-ESCALATION.md. Report architectural gaps
  before departing from the adopted design.
- The generated concept is a visual reference, not live data, entitlement proof,
  a tested UI, or user acceptance. Use real discovery and native controls.

## Implementation phases and deliverables

1. Establish the dependency-free Core package, tests, diagnostics, checked-in
   Xcode app project/shared scheme, and canonical `bash scripts/gate.sh`.
2. Build the native single-window checkpoint: content discovery, Workshop/DLC
   views, settings/folder selection, launch mode/options, presets, native launch
   adapter and honest errors. Core checks must work before extending the GUI.
3. Run the complete canonical gate sequentially. Run read-only diagnostics
   against the real Steam install and summarize actual content/modes without
   publishing personal path dumps or account IDs.
4. Return candidate scope, full raw log locations and exit codes, actual test
   count, unresolved concerns and instructions locating the built `.app`.

Do not launch the real game as part of unit tests or diagnostics. Native UI and
game smoke are the lead's next stage after submission/review. Tests use temporary
synthetic fixtures; never mutate the installed game or Steam configuration.

## Acceptance and check plan

Acceptance A1–A5 is defined in the adopted ARCH-001 revision. The worker provides
evidence for A1–A3 and planner portions of A4; independent QA, real-game loading
evidence and user acceptance cannot be self-certified. Do not mark this task
Done or imply the test app is accepted merely because it compiles.

- Gate from repository root, build/test logs under ignored `.build/evidence/`.
- Format and warnings-as-errors are required; do not suppress unexpected failures.
- Re-read game/content readiness immediately before handoff; prevent duplicate
  launches and do not confuse a process start with successful addon loading.
- Persistence failures preserve the player's data and are visible in the UI.
- Swift source/scripts count toward 800 soft/1400 hard physical-line limits.
- No fabricated owned-DLC labels, download progress, or launch-success states.

## Limits and recovery

Route: native sol_worker / gpt-5.6-sol / medium / fork_turns none.
Initial allowance: 45 minutes, 120 direct tool calls, one exclusive mutation lane.
Send a short progress report after Core checks pass and whenever blocked.
First implementation attempt; bounded repair policy applies to subsequent
rejected submissions. Preserve actual failure receipts and do not weaken tests.
No invented token/cost telemetry. Time limits trigger a precise partial-state
report, never false completion. Reviewer capacity is reserved separately.
