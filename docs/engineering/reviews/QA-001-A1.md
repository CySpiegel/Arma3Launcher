# QA-001-A1 — first candidate independent review

Verdict: **changes required**. Reviewer: `/root/candidate_qa`, Sol Medium,
independent of `/root/implementation`. Exact candidate manifest verified:
`e76340639be40918568fbed29a0ae0d32f86856cdd88b69b53625dbb566cc1b8`.
Current scoped source matched the parked archive/manifest at review. No writes,
build, app/game launches or Git operations. Read-only receipts exited 0.

1. **QA1-01, High — duplicate Workshop records crash.** SteamDiscovery.swift:231
   uses `Dictionary(uniqueKeysWithValues:)` on parser entries that preserve
   duplicates. Duplicate numeric IDs in either Workshop section trap instead of
   yielding unknown state/diagnostics. Detect ambiguity without trapping and
   fixture duplicate installed and detail records.
2. **QA1-02, High — empty DLC becomes launchable.** SteamDiscovery.swift:295–299
   falls back to ready game status whenever the optional DLC folder exists even
   without package evidence. Require positive packages for ready; fixture empty,
   nonpackage, package-bearing and non-ready-game cases.
3. **QA1-03, High — configuration persistence loses edits.** MainWindow.swift:239
   calls persist for general edits but LauncherModel.swift:255 serializes stale
   presets without synchronizing configuration; only toggles synchronize. The
   stored model has no independent current configuration. Save every configuration
   edit to the selected preset and persist current configuration with no preset;
   round-trip mode/options/advanced/selections and both preset states.
4. **QA1-04, High — Play/refresh race and ineffective cancellation.**
   LauncherModel.swift:84 cancels only a parent waiting on detached discovery;
   discovery never checks cancellation. Play's untracked scan at 170–177 has no
   generation guard, allowing stale publication and launch after Refresh. Startup
   and activation duplicate scans. Use one owned scan lifecycle with cancellation
   propagation and freshness checks before publish/plan/handoff; test reordered
   completions and refresh during Play.
5. **QA1-05, High — duplicate precedence/metadata depend on iteration.**
   SteamDiscovery.swift:66 preserves VDF order for remaining roots instead of
   lexicographic path order. Line 208 overwrites names from every copy. Preserve
   explicit/default precedence, sort remaining normalized roots, and read metadata
   from the selected copy. Fixture reversed VDF order, aliases, readiness and names.
6. **QA1-06, Medium — config regex reads comments and incomplete escapes.**
   SteamDiscovery.swift:267 can match names inside block comments and only
   unescapes quotes. Add a bounded comment/string-aware literal scanner with CRLF,
   comments, slash/quote/control escapes, nonliteral values and fallback tests.
7. **QA1-07, Medium — metadata size limits incomplete.** ConfigurationStore.swift:45
   reads without a bound; SteamDiscovery.swift:153 uses Bundle Info.plist loading
   without the adopted 1 MiB bound. Check size before decoding/loading and fixture
   oversized data with recoverable errors.
8. **QA1-08, High — gate checks historical text and misses Package.swift.**
   gate.sh:13 grep matches nested revision 9 irrespective of actual current state;
   matching an attempt ID proves no binding. Directory-only line counts omit the
   root package file. Parse JSON structurally, verify adopted current inputs, and
   include Package.swift and negative gate fixtures. A JSON parser such as Python
   is acceptable; jq was the reviewer's example, not a dependency requirement.
9. **QA1-09, Medium — missing selected content hidden.** MainWindow.swift:259
   displays only selection count; missing IDs appear only in Advanced/Play error.
   Show explicit missing selections in ordinary summary and fixture disappearance.
10. **QA1-10, High — regression matrix incomplete.** Eighteen test functions omit
    duplicate ACFs; legacy/ordered roots; literal metadata; unknown/updating/
    unsupported Workshop items; empty DLC; progress readiness combinations;
    associated manual-folder evidence; duplicate fallback order; all unsafe path
    characters; persistence write errors; model persistence/concurrency. Add
    meaningful failure assertions and preserve existing tests.

Lead disposition: all findings accepted for repair; no waiver or deferral. The
gate's extra local-link and physical-final-line obligations remain per ARCH-002.
Archive/texture tests are additional to this report. Original A4 real-game evidence,
A5 native user acceptance and remote integration remain incomplete.
